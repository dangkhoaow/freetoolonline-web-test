LED is the backlight; LCD is the panel. On almost every modern screen, both names describe the same hardware - so an "LED test" and an "LCD test" run the same color cycle and surface the same defects. Read why the names diverged and which tool to open.