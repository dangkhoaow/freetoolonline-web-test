CSS box-shadow generator - drag offset, blur, spread, color, and opacity sliders and copy the ready-to-paste box-shadow line; use this when you need a drop or inset shadow, not a gradient background (see the CSS Gradient Generator for that).