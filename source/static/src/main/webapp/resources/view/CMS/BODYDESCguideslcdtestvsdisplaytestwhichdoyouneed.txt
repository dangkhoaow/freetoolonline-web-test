Three names, three different checks. LCD test = panel pixels and uniformity. Display test = panel + signal path. Monitor test = panel + signal + ports + scaling. Which to run when, and the right tool to start with.
